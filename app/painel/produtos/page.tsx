import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Plus } from 'lucide-react'
const products = [['Cappuccino clássico','Café, leite vaporizado e canela','R$ 12,50'],['Latte de caramelo','Espresso, leite e caramelo artesanal','R$ 14,00'],['Bolo de cenoura','Fatia com cobertura de chocolate','R$ 11,00'],['Cookie da casa','Cookie crocante com gotas de chocolate','R$ 8,50']]
export default function ProdutosPage() { return <div className="flex flex-col gap-6"><div className="flex items-end justify-between"><div><p className="text-sm font-semibold text-primary">Cardápio</p><h1 className="font-display text-3xl font-800">Produtos</h1><p className="mt-1 text-sm text-muted-foreground">Gerencie o que a casa oferece.</p></div><Button><Plus data-icon="inline-start" /> Novo produto</Button></div><div className="grid gap-4 md:grid-cols-2">{products.map(([name, desc, price]) => <Card key={name} className="rounded-2xl"><CardHeader><div className="flex items-center justify-between"><CardTitle className="font-display">{name}</CardTitle><Badge>Ativo</Badge></div></CardHeader><CardContent className="flex items-center justify-between pt-0"><p className="max-w-xs text-sm leading-6 text-muted-foreground">{desc}</p><span className="font-display font-700 text-primary">{price}</span></CardContent></Card>)}</div></div> }
